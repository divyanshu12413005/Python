import math
l=[1.5,2.5,3.5,4.5]
a=math.fsum(l)    # fsum function only works with iterables like list, tuple, etc and it returns the precise floating point value of the sum of all the elements present in the iterable
print(a)